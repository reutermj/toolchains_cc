name: Build Clang

on:
    push:
        branches: [ clang ]

jobs:
    build:
        runs-on: ubuntu-latest
        container:
            image: alpine:latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            
            - name: what?
              run: ls
            
            - name: Build LLVM
              run: ./build_llvm_linux.sh
