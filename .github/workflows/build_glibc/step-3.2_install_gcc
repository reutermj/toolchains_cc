#!/bin/bash
set -euox pipefail
source ${SCRIPTS_DIR}/environment

GCC_BOOTSTRAP_ARCHIVE="/tmp/x86_64-linux-x86_64-linux-gnu-gcc-bootstrap-15.2.0-20251230.tar.xz"

tar -xf "${GCC_BOOTSTRAP_ARCHIVE}" -C "${SYSROOT_DIR}"
